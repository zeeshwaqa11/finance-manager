{
  "name": "finance-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Personal Finance Manager: React + Express + SQLite, runs entirely locally",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "reset": "npm run reset --prefix server",
    "reset:empty": "npm run reset:empty --prefix server",
    "test": "npm test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
